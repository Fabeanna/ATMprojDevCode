const { balance } = require("./account");

console.log( 'hello');
module.exports = {
    add: userInput + balance
    subtract: userInput - balance
    value: this.value
}

const userInput = require("prompt-sync")()

console.log('Enter your pin!');
